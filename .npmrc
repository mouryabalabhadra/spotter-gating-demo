# The corporate JFrog proxy (jfrog-colo.corp.thoughtspot.com) was returning 503
# for every public package. Everything this demo needs from a registry is public,
# and the embed SDK comes from a local link — so go straight to npmjs.
# Delete this file once the proxy is healthy again.
registry=https://registry.npmjs.org/
